#include<stdio.h>
#include<math.h>
int main()
{
 int number;
 int sum=0;
 int i=0;
 printf("Enter the number:");
 scanf("%d",&number);
 while(number>0)
 {
  sum+=(number%10)*pow(2,i);
  number 1=10;
  i++;
 }
 printf("The decimal value of Binary number is %d",sum);
 return 0;
}
